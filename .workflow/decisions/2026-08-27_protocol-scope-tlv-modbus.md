# 2026-08-27 协议解析器首版范围：TLV 自定义 + Modbus RTU

- **背景**：BMS 上位机用 0x5A 帧头 + CRC16-Modbus 自定义协议，EMS 用 0xAAAA 帧头 + 命令字 + CRC16，均非标准 Modbus。
- **结论**：首版同时支持①可配置 TLV 自定义帧（帧头/地址/命令字/长度/CRC16，字段表驱动，附 BMS、EMS 预设）②标准 Modbus RTU（01/02/03/04/0F/10 + 寄存器映射）。
- **影响**：协议引擎设计为「帧结构描述 + 字段表」通用模型，两种协议都是其实例。
