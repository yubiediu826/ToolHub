# 2026-08-27 单主项目 + git 初始化

- **结论**：以 `D:\WorkSpace\ToolHub\ToolHub` 为唯一主项目；`Example/` 仅作 FluentUI 组件参考不再维护。主项目 git init 并首次提交。
- **gitignore**：venv/、build/、dist/、__pycache__/、*.qm、*.qmlc、resource_rc.py、.workflow/_archive/。
